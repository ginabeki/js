let first_name = "Gina";
let last_name = "Bobby";
let age = 45;
let first = true;
let last = false;
let bob; // undefined cos nothing is declared

if (age <= 45) {
  // if the value of age is less than or equal to 40
} else if (age > 40 && age < 50) {
  // if age is greater than 40 and less than 50
  // && - means AND
  // || - means OR
} else if (age < 0) {
  // otherwise prevent something from hapening
} else {
  //greater than 50
}

// Comparison operators
// < <= > >= == != ===
